# 📸 Поддержка фото и видео

## ✨ Новая функция!

Теперь пользователи могут отправлять **фото и видео** в Telegram боте, и менеджеры будут видеть их в панели управления!

---

## 🎯 Как это работает

### Для пользователей:

1. Откройте бота в Telegram
2. Отправьте фото или видео (можно с подписью)
3. Медиа автоматически добавится в ваш тикет

### Для менеджеров:

1. Откройте панель управления (http://localhost:5173)
2. Выберите тикет
3. Видео медиа прямо в чате:
   - **Фото** - кликабельно, открывается в полном размере
   - **Видео** - встроенный плеер с контролами

---

## 🏗️ Архитектура решения

### Метод хранения: Telegram URLs (Вариант 1)

**Как работает:**
1. Пользователь отправляет фото/видео в бота
2. Бот получает `file_id` от Telegram
3. Бот запрашивает `file_path` (URL файла)
4. URL сохраняется в базу данных
5. Менеджер видит медиа по этому URL

### Преимущества:
- ✅ Быстрая реализация (30 минут)
- ✅ Не нужно свое хранилище
- ✅ Файлы бесплатно у Telegram
- ✅ Не нагружает наш сервер

### Ограничения:
- ⚠️ Файлы доступны 1-2 года (Telegram может удалить старые)
- ⚠️ Зависимость от Telegram
- ⚠️ Требуется токен бота для доступа к приватным файлам

---

## 📊 Изменения в базе данных

### Таблица `messages` - добавлены поля:

```sql
media_type VARCHAR(20)      -- 'photo' или 'video'
media_url TEXT              -- URL файла из Telegram
media_file_id VARCHAR(255)  -- file_id от Telegram (на будущее)
```

### Миграция существующей БД:

```bash
cd backend
python migrate_media.py
```

Этот скрипт автоматически добавит новые поля в таблицу `messages`.

---

## 🔧 Технические детали

### Backend изменения:

**server.py:**
- Обновлена модель `AddMessageRequest` (добавлены поля медиа)
- Обновлен SQL запрос для сохранения медиа
- Схема БД обновлена с новыми полями

**bot.py:**
- Добавлен `photo_handler` - обработка фото
- Добавлен `video_handler` - обработка видео
- Обновлена `add_message_to_ticket` - передача медиа данных
- Обновлена `create_ticket` - поддержка pending_media

**migrate_media.py:**
- Скрипт миграции БД
- Добавляет новые колонки безопасно

### Frontend изменения:

**App.jsx:**
- Добавлено отображение фото (кликабельно)
- Добавлено отображение видео (встроенный плеер)
- Автоматическое определение типа медиа

---

## 🚀 Запуск после обновления

### Шаг 1: Миграция БД
```bash
cd backend
python migrate_media.py
```

### Шаг 2: Перезапуск backend
```bash
# Остановите текущие процессы (Ctrl+C в каждом окне)
cd ..
.\start-all.bat
```

### Шаг 3: Проверка
1. Откройте бота в Telegram
2. Отправьте фото или видео
3. Откройте панель менеджера
4. Убедитесь, что медиа отображается

---

## 📸 Поддерживаемые форматы

### Фото:
- JPG, PNG, WEBP
- Максимальный размер: 10 MB
- Максимальное разрешение: 10000x10000 px

### Видео:
- MP4, MOV, AVI
- Максимальный размер: 50 MB
- Максимальная длительность: без ограничений

*Ограничения установлены Telegram*

---

## 💡 Примеры использования

### Пользователь отправляет фото проблемы:
```
Пользователь: [Фото сломанного товара]
             "Товар пришел поврежденным"
             
Менеджер видит: 
- Фото в полном размере
- Подпись "Товар пришел поврежденным"
```

### Пользователь отправляет видео:
```
Пользователь: [Видео не работающего приложения]
             "Приложение зависает на этом экране"
             
Менеджер видит:
- Встроенный видеоплеер
- Может воспроизвести и скачать
```

---

## 🔮 Будущие улучшения

### Возможные доработки:

1. **Собственное хранилище**
   - S3, MinIO, или локальное
   - Постоянное хранение файлов
   - Независимость от Telegram

2. **Больше типов файлов**
   - Документы (PDF, DOCX)
   - Аудио
   - Голосовые сообщения

3. **Отправка файлов менеджером**
   - Загрузка файлов через UI
   - Отправка клиенту

4. **Превью и оптимизация**
   - Генерация thumbnail
   - Сжатие изображений
   - Lazy loading

5. **Галерея**
   - Просмотр всех медиа тикета
   - Скачивание всех файлов

---

## ❓ FAQ

### Q: Как долго хранятся файлы в Telegram?
**A:** Обычно 1-2 года, но Telegram может удалить старые файлы раньше.

### Q: Можно ли скачать файлы?
**A:** Да, кликните по фото или используйте контролы видео для скачивания.

### Q: Что если файл удален из Telegram?
**A:** Медиа не отобразится, будет показана ошибка. Рекомендуется внедрить собственное хранилище для важных файлов.

### Q: Можно ли отправлять несколько файлов?
**A:** Да, отправляйте по одному, каждый файл будет отдельным сообщением.

### Q: Поддерживаются ли другие типы файлов?
**A:** Пока только фото и видео. Документы и аудио планируются в будущем.

---

## 🛠️ Для разработчиков

### API для сохранения медиа:

```javascript
POST /api/tickets/{ticket_id}/messages
{
  "senderType": "client",
  "senderId": "123456",
  "content": "Описание проблемы",
  "mediaType": "photo",           // или "video"
  "mediaUrl": "https://...",       // URL файла
  "mediaFileId": "AgACAgIAAx..."  // file_id от Telegram
}
```

### Пример получения file_path в Python:

```python
from telegram import Update
from telegram.ext import ContextTypes

async def photo_handler(update: Update, context: ContextTypes.DEFAULT_TYPE):
    photo = update.message.photo[-1]  # Максимальное разрешение
    file = await context.bot.get_file(photo.file_id)
    media_url = file.file_path
    media_file_id = photo.file_id
    # Сохраните в БД
```

---

## ✅ Готово!

Система теперь полностью поддерживает фото и видео! 🎉

**Обратная связь:** Создайте issue если нашли баги или есть предложения.

