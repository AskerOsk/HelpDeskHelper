# FAQ - Часто задаваемые вопросы

## 🚀 Установка и запуск

### Как быстро запустить проект?

**Локально:**
```bash
.\start-all.bat
```

**Docker:**
```bash
docker-compose up -d
```

### Где взять токен Telegram бота?

1. Найдите в Telegram: @BotFather
2. Отправьте команду `/newbot`
3. Следуйте инструкциям
4. Скопируйте полученный токен
5. Вставьте в `backend/.env` или `.env` (для Docker)

### Проект не запускается, что делать?

1. Проверьте установлены ли все зависимости:
   ```bash
   # Backend (Python)
   cd backend
   python -m pip install -r requirements.txt
   
   # Frontend (Node.js)
   cd ../frontend
   npm install
   ```

2. Проверьте PostgreSQL:
   ```bash
   psql -U postgres
   ```

3. Проверьте переменные окружения в `.env`

4. Проверьте, не заняты ли порты 3001, 3002, 5173

## 🐛 Типичные проблемы

### "ModuleNotFoundError: No module named 'fastapi'" или 'telegram'

**Причина:** Python зависимости не установлены.

**Решение (Быстрое):**
```bash
cd backend
python -m pip install -r requirements.txt
```

**Решение (С виртуальным окружением - рекомендуется):**
```bash
cd backend
python -m venv venv
venv\Scripts\activate
pip install --upgrade pip
pip install -r requirements.txt
```

После установки запустите снова:
```bash
cd ..
.\start-all.bat
```

**Важно:** Если вы создали виртуальное окружение, его нужно активировать перед каждым запуском:
```bash
cd backend
venv\Scripts\activate
```

Или используйте упрощенный скрипт установки:
```bash
.\install-backend.bat
```

### "Port 3001 already in use"

**Windows:**
```powershell
Get-Process -Name node | Stop-Process -Force
```

**Или освободить конкретный порт:**
```powershell
netstat -ano | findstr :3001
taskkill /PID <PID> /F
```

### "Database connection failed"

1. Убедитесь, что PostgreSQL запущен
2. Проверьте настройки в `.env`:
   - DB_HOST=127.0.0.1 (для локального)
   - DB_HOST=postgres (для Docker)
3. Создайте базу данных:
   ```sql
   CREATE DATABASE sulpak_helpdesk;
   ```

### "Bot not responding"

1. Проверьте токен в `.env`
2. Убедитесь, что backend запущен (порт 3001)
3. Проверьте логи:
   - Локально: смотрите консоль
   - Docker: `docker-compose logs backend`

### Frontend не подключается к Backend

1. Проверьте, что backend запущен на порту 3001
2. Проверьте CORS настройки в `backend/server.js`
3. Для Docker: используйте `BACKEND_URL=http://backend:3001`
4. Для локального: `BACKEND_URL=http://localhost:3001`

## 🐳 Docker вопросы

### Как пересобрать образы?

```bash
docker-compose build --no-cache
```

### Как посмотреть логи?

**Все сервисы:**
```bash
docker-compose logs -f
```

**Конкретный сервис:**
```bash
docker-compose logs -f backend
docker-compose logs -f frontend
docker-compose logs -f postgres
```

### Как полностью очистить и перезапустить?

```bash
# Остановить и удалить все контейнеры и volumes
docker-compose down -v

# Пересобрать и запустить заново
docker-compose up -d --build
```

### Как зайти внутрь контейнера?

```bash
# Backend
docker exec -it sulpak_backend sh

# Postgres
docker exec -it sulpak_postgres psql -U postgres -d sulpak_helpdesk
```

## 💾 База данных

### Как создать базу данных вручную?

```bash
psql -U postgres
CREATE DATABASE sulpak_helpdesk;
\q
```

### Как инициализировать таблицы?

Таблицы создаются автоматически при первом запуске `server.js`.

Или вручную:
```bash
cd backend
node create-db.js
```

### Как посмотреть данные в БД?

```bash
psql -U postgres -d sulpak_helpdesk
```

Полезные команды:
```sql
\dt                              -- список таблиц
SELECT * FROM tickets;           -- все тикеты
SELECT * FROM messages;          -- все сообщения
SELECT * FROM managers;          -- все менеджеры
```

### Как очистить все данные?

```sql
TRUNCATE TABLE messages, tickets, managers RESTART IDENTITY CASCADE;
```

## 🔧 Разработка

### Как добавить новый API endpoint?

1. Откройте `backend/server.js`
2. Добавьте новый route:
   ```javascript
   app.get('/api/my-endpoint', async (req, res) => {
     // ваша логика
     res.json({ success: true });
   });
   ```

### Как добавить новую команду в бот?

1. Откройте `backend/bot.js`
2. Добавьте обработчик:
   ```javascript
   bot.onText(/\/mycommand/, async (msg) => {
     const chatId = msg.chat.id;
     await bot.sendMessage(chatId, 'Ответ');
   });
   ```

### Как изменить стили Frontend?

1. Откройте `frontend/src/index.css`
2. Используйте Tailwind классы или добавьте кастомные стили
3. Изменения применятся автоматически (hot reload)

### Где логи приложения?

**Локально:**
- Backend: консоль где запущен `npm start`
- Bot: консоль где запущен `npm run bot`
- Frontend: консоль где запущен `npm run dev`

**Docker:**
```bash
docker-compose logs -f
```

## 📦 Деплой

### Как собрать для production?

**Frontend:**
```bash
cd frontend
npm run build
# Результат в frontend/dist/
```

**Docker:**
```bash
docker-compose -f docker-compose.yml up -d --build
```

### Какие порты нужно открыть на сервере?

- **80** - Frontend (HTTP)
- **443** - Frontend (HTTPS, если настроен SSL)
- **3001** - Backend API (или закрыть и использовать Nginx proxy)
- **5432** - PostgreSQL (только для локального доступа, не открывать публично)

### Как настроить HTTPS?

1. Получите SSL сертификат (Let's Encrypt)
2. Обновите `nginx.conf`:
   ```nginx
   server {
       listen 443 ssl;
       ssl_certificate /path/to/cert.pem;
       ssl_certificate_key /path/to/key.pem;
       ...
   }
   ```

## 🔐 Безопасность

### Что нельзя коммитить в Git?

- `.env` файлы с токенами и паролями
- `node_modules/`
- Логи (`*.log`)
- IDE настройки (`.idea/`, `.vscode/`)

### Как защитить панель менеджера?

Добавьте аутентификацию:
1. JWT токены
2. Passport.js
3. OAuth (Google, etc.)

### Как изменить пароль базы данных?

1. Измените в `.env`:
   ```env
   DB_PASSWORD=новый_сильный_пароль
   ```
2. Обновите пароль в PostgreSQL:
   ```sql
   ALTER USER postgres WITH PASSWORD 'новый_сильный_пароль';
   ```

## 📊 Мониторинг

### Как проверить статус сервисов?

**Docker:**
```bash
docker-compose ps
```

**Локально:**
```powershell
Get-Process -Name python
```

### Как проверить использование ресурсов?

```bash
docker stats
```

## 🆘 Помощь

### Где найти больше информации?

- **[README.md](../README.md)** - главная страница проекта
- **[START_HERE.md](START_HERE.md)** - быстрый старт за 3 шага
- **[QUICKSTART.md](QUICKSTART.md)** - детальная инструкция
- **[PROJECT_STRUCTURE.md](PROJECT_STRUCTURE.md)** - архитектура проекта

### Как сообщить об ошибке?

Создайте issue в репозитории с описанием:
1. Что вы пытались сделать
2. Что произошло
3. Логи ошибки
4. ОС и версия Python/Node.js

### Нужна помощь с настройкой?

Свяжитесь с командой разработки через:
- Email
- Telegram
- Issues в репозитории

