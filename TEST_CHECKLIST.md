# 🧪 Test Checklist - AI HelpDesk

## Pre-Test Setup

### ✅ 1. Ollama готова
```bash
# Проверьте что Ollama запущена
ollama ps

# Если модель не загружена, загрузите
ollama pull qwen2.5:3b

# Проверьте список моделей
ollama list
```

**Ожидаемый результат:**
```
NAME            ID              SIZE    MODIFIED
qwen2.5:3b      xxx             2.0 GB  X minutes ago
```

---

### ✅ 2. PostgreSQL запущен
```bash
# Windows: проверьте сервис
Get-Service postgresql*

# Или попробуйте подключиться
psql -U postgres -h 127.0.0.1 -p 5432 -d sulpak_helpdesk
```

**Ожидаемый результат:** Подключение успешно

---

### ✅ 3. База данных инициализирована
```bash
cd backend
python create_db.py
```

**Ожидаемый результат:**
```
Database 'sulpak_helpdesk' created or already exists
Tables created successfully
```

---

### ✅ 4. Dependencies установлены
```bash
# Backend
cd backend
pip install -r requirements.txt

# Frontend
cd frontend
npm install
```

---

### ✅ 5. Environment variables настроены

Проверьте `backend/.env`:
```bash
# Должны быть установлены:
TELEGRAM_BOT_TOKEN=your_token
USE_OLLAMA=true
OLLAMA_URL=http://localhost:11434
AI_MODEL=qwen2.5:3b
MANAGER_EMAIL=asker.kaudinov@gmail.com
```

---

## Test Scenarios

### 🧪 Test 1: Базовый AI ответ

**Шаги:**
1. Запустите backend:
   ```bash
   cd backend
   python run_all.py
   ```

2. Проверьте что все сервисы запустились:
   - ✅ API Server (port 3001)
   - ✅ Webhook Server (port 3002)
   - ✅ Telegram Bot

3. Откройте Telegram, напишите боту: `/start`

4. Нажмите "➕ Новый запрос"

5. Отправьте сообщение:
   ```
   Здравствуйте! Как я могу вернуть товар?
   ```

**Ожидаемый результат:**
- ✅ Бот подтверждает создание тикета
- ✅ В течение 5-10 секунд приходит ответ от "🤖 AI Ассистент"
- ✅ Ответ на русском языке
- ✅ Ответ про возврат товара (политика 14 дней и т.д.)

**Логи для проверки:**
```bash
# backend/server.log должен содержать:
[INFO] Generating AI response for ticket #...
[INFO] AI response generated | Confidence: 0.XX
[INFO] AI response sent to user ...
```

---

### 🧪 Test 2: Многоходовая беседа

**Шаги:**
1. Продолжая предыдущий тест, отправьте:
   ```
   А какие документы нужны для возврата?
   ```

2. Дождитесь ответа AI

3. Отправьте еще:
   ```
   Спасибо, все понятно!
   ```

**Ожидаемый результат:**
- ✅ AI отвечает на каждое сообщение
- ✅ AI помнит контекст беседы
- ✅ Ответы релевантны вопросам
- ✅ На "Спасибо" AI должен закрыть беседу вежливо

---

### 🧪 Test 3: Просмотр в UI (Monitoring Dashboard)

**Шаги:**
1. Запустите frontend (в отдельном терминале):
   ```bash
   cd frontend
   npm run dev
   ```

2. Откройте браузер: http://localhost:5173

3. Проверьте UI:
   - ✅ Видны созданные тикеты в левой панели
   - ✅ При клике на тикет - полная история беседы
   - ✅ Видно "👤 User" и "🤖 AI" сообщения
   - ✅ Timestamps корректные

---

### 🧪 Test 4: AI Escalation (Эскалация)

**Цель:** Проверить что AI передает сложные вопросы менеджеру

**Шаги:**
1. Создайте новый тикет в Telegram

2. Отправьте сложный/специфичный вопрос:
   ```
   Здравствуйте! Я купил телевизор Samsung QE55Q80C три дня назад по номеру заказа #123456789. Сегодня на экране появилась вертикальная полоса. Хочу вернуть деньги немедленно!
   ```

3. Дождитесь ответа AI

**Ожидаемый результат:**
- ✅ AI отвечает, но с низкой уверенностью
- ✅ AI предлагает связаться с менеджером
- ✅ В логах: `Escalating due to low confidence`
- ✅ Email отправлен на asker.kaudinov@gmail.com

**Проверка email:**
- Откройте Gmail: asker.kaudinov@gmail.com
- ✅ Должно прийти письмо с темой: "🚨 Sulpak HelpDesk - Escalation Required"
- ✅ Email содержит:
  - Номер тикета
  - Полную историю беседы
  - AI резюме проблемы
  - Красивый HTML дизайн

---

### 🧪 Test 5: Медиа поддержка

**Шаги:**
1. Создайте новый тикет

2. Отправьте фото (например, скриншот проблемы)

3. Добавьте подпись:
   ```
   Вот проблема с телефоном
   ```

**Ожидаемый результат:**
- ✅ Фото сохранено в базе
- ✅ AI получает контекст "[PHOTO] Вот проблема с телефоном"
- ✅ AI отвечает учитывая, что пользователь отправил фото
- ✅ В UI видно фото

---

### 🧪 Test 6: Статусы тикетов

**Шаги:**
1. Проверьте статусы в UI (frontend)

**Ожидаемые статусы:**
- ✅ `new` - новый тикет (до AI ответа)
- ✅ `ai_processing` - AI обрабатывает
- ✅ `resolved` - AI решил проблему
- ✅ `escalated` - передано менеджеру

**Проверка в БД:**
```sql
SELECT id, ticket_number, status, ai_summary, escalated_at
FROM tickets
ORDER BY created_at DESC
LIMIT 5;
```

---

### 🧪 Test 7: Performance Test

**Цель:** Убедиться что система быстрая

**Шаги:**
1. Создайте 3 тикета подряд (с разными вопросами)

2. Измерьте время от отправки сообщения до получения AI ответа

**Ожидаемые показатели:**
- ✅ Первый ответ: 5-15 секунд (модель загружается)
- ✅ Последующие: 2-5 секунд
- ✅ API Server response time < 100ms (для не-AI endpoints)

**Проверка Ollama load:**
```bash
ollama ps
# Должна показать модель в памяти
```

---

### 🧪 Test 8: Error Handling

**Тест 8.1: Ollama остановлена**

**Шаги:**
1. Остановите Ollama:
   ```bash
   # Windows: остановите сервис
   Stop-Service Ollama
   ```

2. Попробуйте создать тикет

**Ожидаемый результат:**
- ✅ Тикет создается
- ✅ В логах ошибка: "Error generating AI response"
- ✅ Автоматическая эскалация (отправляется email менеджеру)
- ✅ Пользователь получает fallback сообщение

3. Запустите Ollama обратно:
   ```bash
   Start-Service Ollama
   ```

---

**Тест 8.2: База данных недоступна**

**Шаги:**
1. Остановите PostgreSQL

2. Попробуйте создать тикет

**Ожидаемый результат:**
- ✅ HTTP 500 или 503 ошибка
- ✅ В логах: Database connection error
- ✅ Health endpoint показывает "unhealthy"

---

### 🧪 Test 9: Concurrent Users

**Шаги:**
1. Создайте тикеты от 2-3 разных Telegram аккаунтов одновременно

**Ожидаемый результат:**
- ✅ Все тикеты обрабатываются независимо
- ✅ AI отвечает на каждый
- ✅ Нет смешивания контекста между пользователями

---

### 🧪 Test 10: System Restart

**Шаги:**
1. Остановите все сервисы (Ctrl+C)

2. Перезапустите:
   ```bash
   cd backend
   python run_all.py
   ```

3. Проверьте `/start` в Telegram боте

4. Проверьте "📋 Мои запросы" - должны быть старые тикеты

**Ожидаемый результат:**
- ✅ Все сервисы запускаются без ошибок
- ✅ База данных подключена
- ✅ История тикетов сохранена
- ✅ Сессии пользователей восстановлены из БД

---

## Troubleshooting Guide

### ❌ AI не отвечает

**Причины:**
1. Ollama не запущена → `ollama serve`
2. Модель не загружена → `ollama pull qwen2.5:3b`
3. Неправильный URL → проверьте `.env`: `OLLAMA_URL=http://localhost:11434`

**Логи для диагностики:**
```bash
tail -f backend/server.log | grep "AI"
```

---

### ❌ Email не отправляется

**Причины:**
1. SMTP credentials не настроены → заполните `.env`
2. Gmail блокирует → создайте App Password
3. Email service disabled → проверьте `email_service.py` initialization

**Тест SMTP:**
```python
# backend/test_email.py
import asyncio
from email_service import get_email_service

async def test():
    service = get_email_service()
    result = await service.send_escalation_email(
        ticket_number="TEST123",
        ticket_id=1,
        user_info={"telegram_username": "test", "telegram_user_id": 123},
        conversation_history=[],
        ai_summary="Test escalation"
    )
    print(f"Email sent: {result}")

asyncio.run(test())
```

---

### ❌ Telegram bot не отвечает

**Причины:**
1. Неправильный токен → проверьте `.env`
2. Bot не запущен → проверьте `python run_all.py` запустил bot.py
3. Network issue → проверьте интернет

**Тест webhook:**
```bash
curl http://localhost:3002/health
# Должен вернуть {"status": "ok"}
```

---

## Success Criteria

✅ **Система готова к production если:**

1. ✅ Все 10 тестов пройдены
2. ✅ AI отвечает в течение 5 секунд
3. ✅ Email эскалация работает
4. ✅ UI показывает все беседы корректно
5. ✅ Система переживает рестарт без потери данных
6. ✅ Error handling работает (graceful degradation)
7. ✅ Логи не содержат критических ошибок

---

## Next Steps After Testing

После успешного тестирования:

1. **Task #8**: Создать красивый UI с dark theme
2. **Production deployment**: Docker Compose setup
3. **Monitoring**: Prometheus + Grafana
4. **Analytics**: Добавить дашборд с метриками

---

## Quick Commands

```bash
# Полный рестарт системы
# 1. Остановите все (Ctrl+C)
# 2. Очистите БД (опционально)
cd backend && python create_db.py && cd ..

# 3. Запустите backend
cd backend && python run_all.py

# 4. В новом терминале - frontend
cd frontend && npm run dev

# Проверка здоровья
curl http://localhost:3001/health  # API
curl http://localhost:3002/health  # Webhook
ollama ps                           # Ollama
```

---

**Готовы тестировать!** 🚀
