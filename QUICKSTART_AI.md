# 🚀 Quick Start - AI HelpDesk

Быстрая инструкция для запуска AI-powered HelpDesk с Ollama.

## ⚡ За 5 минут

### 1. Установите Ollama

```bash
# Windows: скачайте и установите
https://ollama.com/download/OllamaSetup.exe

# Или через winget
winget install Ollama.Ollama
```

### 2. Установите модель Qwen 2.5

```bash
ollama pull qwen2.5:3b
```

Ждите ~3-5 минут (скачается ~2GB).

### 3. Проверьте установку

```bash
ollama list
# Должны увидеть: qwen2.5:3b

ollama ps
# Должен показать запущенные модели
```

### 4. Установите зависимости проекта

```bash
# Установите все зависимости
.\install-all.bat

# Или вручную:
.\install-backend.bat
.\install-frontend.bat
```

### 5. Настройте `.env`

Файл `backend/.env` уже настроен с Ollama! Проверьте:

```bash
USE_OLLAMA=true
OLLAMA_URL=http://localhost:11434
AI_MODEL=qwen2.5:3b
TELEGRAM_BOT_TOKEN=your_token_here  # Замените на ваш токен
```

### 6. Инициализируйте базу данных

```bash
cd backend
python create_db.py
cd ..
```

### 7. Запустите проект

```bash
# Запустите backend (все 3 сервиса)
cd backend
python run_all.py

# В отдельном терминале - frontend
cd frontend
npm run dev
```

### 8. Тестируйте!

1. Откройте Telegram, напишите вашему боту
2. Отправьте сообщение (минимум 10 символов)
3. AI автоматически ответит!
4. Откройте http://localhost:5173 - увидите беседу в UI

## 🎯 Что происходит

```
Пользователь (Telegram)
  ↓ "Здравствуйте, как вернуть товар?"
Telegram Bot
  ↓ POST /api/v1/tickets
API Server
  ↓ Создает тикет, сохраняет сообщение
AI Service (Ollama Qwen 2.5)
  ↓ Генерирует ответ на русском
Webhook Server
  ↓ Отправляет ответ в Telegram
Пользователь получает ответ мгновенно!
```

## 📊 Мониторинг

- **Frontend UI**: http://localhost:5173 (мониторинг всех бесед)
- **API Server**: http://localhost:3001/health
- **Ollama**: `ollama ps` (показать загрузку модели)

## 🔧 Troubleshooting

### Ollama не запущена

```bash
# Запустите вручную
ollama serve
```

### AI отвечает медленно

Это нормально для первого ответа (модель загружается). Последующие ответы быстрее.

**Ускорение:**
- Используйте GPU (если есть NVIDIA)
- Используйте меньшую модель: `ollama pull qwen2.5:1.5b`

### AI отвечает на английском

Проверьте `backend/ai_service.py` - system prompt должен содержать:
```python
"Отвечай дружелюбно, профессионально и по существу на русском языке"
```

### Email не отправляются

Email нужны только при эскалации (когда AI не уверен). Настройте в `.env`:

```bash
SMTP_USERNAME=your_email@gmail.com
SMTP_PASSWORD=your_app_password
MANAGER_EMAIL=manager@example.com
```

Для Gmail:
1. Включите 2FA в аккаунте Google
2. Создайте App Password: https://myaccount.google.com/apppasswords

## 🎨 Следующие шаги

1. **Доработка UI** - создание современного dark theme (Task #8)
2. **Обновление bot.py** - показ AI ответов (Task #6)
3. **Тестирование** - полный E2E тест (Task #11)

## 📚 Полная документация

- `CLAUDE.md` - полная архитектура и roadmap
- `docs/OLLAMA_SETUP.md` - подробная настройка Ollama
- `README.md` - общая информация о проекте

## 🆘 Помощь

Если что-то не работает:

1. Проверьте логи: `backend/server.log`
2. Проверьте Ollama: `ollama ps`
3. Проверьте БД: PostgreSQL должен быть запущен на порту 5432
4. Проверьте порты: 3001 (API), 3002 (Webhook), 5173 (Frontend), 11434 (Ollama)

---

**Готово!** 🎉 Теперь у вас работает AI HelpDesk с автоматическими ответами на русском языке через Ollama Qwen 2.5!
